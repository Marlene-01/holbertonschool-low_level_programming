#include <stdio.h>

/**
 * main - Prints Smile in the mirror
 *
 * Return: Always 0.
 *
 */

int main(void)
{
	int l;

	for (l = 'z'; l < 'a'; l++)
	{
		putchar(l);
	}
	{
		putchar('\n');
	}

	return (0);
}

